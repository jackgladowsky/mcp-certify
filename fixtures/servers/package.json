{
  "name": "mcp-certify-fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Test fixture MCP servers for mcp-certify",
  "scripts": {
    "vulnerable": "tsx vulnerable-server.ts",
    "safe": "tsx safe-server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
